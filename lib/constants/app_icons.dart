import 'package:revogarage/utils/app_libraries.dart';

class AppIcons {
  // <---------------------- Define App Icons Here ------------------------>

  static const Icon facebook = Icon(
    FontAwesomeIcons.facebook,
    color: Colors.blue,
    size: 40,
  );

  static const Icon google = Icon(
    FontAwesomeIcons.google,
    color: Colors.blue,
    size: 40,
  );

  static const Icon backButton = Icon(
    FontAwesomeIcons.arrowLeft,
    color: AppColors.appBarTextColor,
    size: 20,
  );

  static const Icon forwardButton = Icon(
    FontAwesomeIcons.caretRight,
    color: AppColors.buttonColor,
    size: 20,
  );

  static const Icon circleCheck = Icon(
    FontAwesomeIcons.circleCheck,
    color: AppColors.buttonColor,
    size: 200,
  );
}
